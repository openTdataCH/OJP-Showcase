-- frequencies are already imported as stand-alone trips in db, dont't export them again

-- SELECT 
--     frequencies.* 
-- FROM 
--     frequencies,
--     link_filter_trips
-- WHERE
--     frequencies.trip_id = link_filter_trips.trip_id;
